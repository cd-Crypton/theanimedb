{
    "name": "theanimedb",
    "version": "1.0.0",
    "scripts": {
        "build": "npm run copy-files",
        "copy-files": "mkdir -p public && cp -r *.html css js img public/",
        "dev": "wrangler dev",
        "deploy": "npm run build && wrangler deploy && npm install axios",
        "deploy:dry": "npm run build && wrangler deploy --dry-run"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    },
    "dependencies": {
        "axios": "^1.11.0"
    }
}
