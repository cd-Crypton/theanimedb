name = "theanimedb"
main = "public/src/_workers.js"
compatibility_date = "2025-06-05"

# This explicitly binds the 'dist' directory to the 'ASSETS' variable in your worker
assets = { directory = "./dist" }

[vars]
ENVIRONMENT = "production"

[observability]
enabled = true
head_sampling_rate = 1